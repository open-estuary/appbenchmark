# Increase Java Heap as needed to support concurrent
HEAP="{{ eureka_heap_option }}"

#There are GC settings, you can adjust some to make micro services working better
GC_ALGO="{{ eureka_gc_algo_option }}"

# Save memory storge snapshot, the path is /opt/e-commerce/logs/order.oomdump
DUMP="{{ eureka_dump }}"

